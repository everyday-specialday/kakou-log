# 加工条件記録（PWA版）セットアップ手順

このzipには次の7ファイルが入っています。**すべて同じフォルダに置いてください。**

- index.html
- manifest.json
- service-worker.js
- icon-192.png
- icon-512.png
- icon-512-maskable.png
- apple-touch-icon.png

## 重要：ネット上に置く場所が必要です

iPhoneのSafariでオフライン動作（Service Worker）を使うには、**https:// のアドレスとして公開されている必要があります**。
ファイルをそのままiPhoneで開く（file://）方式では、Service Workerが登録されず、毎回オンラインが必要な今までの動作のままになってしまいます。

無料で一番簡単なのは **Netlify Drop** です。

1. パソコンのブラウザで https://app.netlify.com/drop を開く
2. 解凍した7ファイルが入ったフォルダを、そのままページにドラッグ＆ドロップする
3. 数秒で `https://（ランダムな文字列）.netlify.app` のようなURLが発行される
4. そのURLをiPhoneのSafariで開く（このとき1回だけネット接続が必要）

ほかに GitHub Pages や Cloudflare Pages でも同様に公開できます。すでにお使いのレンタルサーバーがあれば、そこに7ファイルをアップロードするだけでも構いません。

## iPhoneでの使い方

1. 発行されたURLをSafariで開く（初回のみネット接続が必要）
2. 画面が表示されたら、共有ボタン → 「ホーム画面に追加」を選ぶと、アイコンをタップしてアプリのように起動できます
3. 一度開いたあとは、機内モードや電波の届かない場所でも記録の追加・編集・検索・CSV書き出しがすべて使えます

## データの保存について

- 記録データはこれまで通り、**Safari内（この端末）だけ**に保存されます。他の端末とは共有されません
- Safariの「履歴とWebサイトデータを消去」を行うと、記録も一緒に消えます
- 大事な記録は、アプリ内の「CSV書き出し」で定期的にバックアップを取ることをおすすめします

## アプリを更新したいとき

今後、機能追加や修正をした新しいファイル一式をお渡しした場合は、同じURLの場所に上書きアップロードしてください。次にiPhoneがオンラインになったタイミングで自動的に新しい内容に更新されます。
